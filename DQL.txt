TABLE EMP (
    EMPNO    NUMBER(4)      CONSTRAINT PK_EMP PRIMARY KEY,
    ENAME    VARCHAR2(10),
    JOB      VARCHAR2(9),
    MGR      NUMBER(4),
    HIREDATE DATE,
    SAL      NUMBER(7,2),
    COMM     NUMBER(7,2),
    DEPTNO   NUMBER(2)      CONSTRAINT FK_DEPTNO REFERENCES DEPT (DEPTNO)
);
 
TABLE DEPT (
    DEPTNO    NUMBER(2,0),
    DNAME     VARCHAR2(14),
    LOC       VARCHAR2(13),
    CONSTRAINT PK_DEPT PRIMARY KEY (DEPTNO)
);

1. 
SELECT ENAME, EMPNO,T2.DNAME FROM EMP T1
JOIN DEPT T2
ON T1.DEPTNO = T2.DEPTNO

2. THE DNAME,DEPTNO, TOTAL_NUMBER OF EMP, MAX(SAL) MIN(SAL) 0 MIN 0 MAX WHEN DEPT HAS NO EMP

SELECT T2.DNAME,T2.DEPTNO, COUNT(*) AS TOTAL_EMP FROM EMP T1
JOIN DEPT T2
ON T1.DEPTNO = T2.DEPTNO
GROUP BY T2.DEPTNO

SELECT MAX(SAL), MIN(SAL) FROM EMP
GROUP BY DEPTNO


